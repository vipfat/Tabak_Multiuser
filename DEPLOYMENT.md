# Руководство по развертыванию системы

## 🎯 Что было реализовано

### 1. Архитектура
- **Двухуровневая система**: Владельцы заведений (email/пароль + JWT) + Клиенты (Telegram)
- Подробная документация в [ARCHITECTURE.md](./ARCHITECTURE.md)

### 2. База данных
Новые таблицы:
- `venue_owners` - Владельцы заведений
- `venue_applications` - Заявки на подключение
- `owner_sessions` - JWT сессии
- Обновлена таблица `venues` (добавлены поля owner_id, slug, bowl_capacity, allow_brand_mixing, address)

### 3. Backend API
Созданы новые модули в `/server/`:
- `authService.js` - Хэширование паролей, JWT токены
- `authMiddleware.js` - Middleware для защиты роутов
- `authRoutes.js` - Endpoints авторизации (/api/auth/*)
- `ownerRoutes.js` - Endpoints личного кабинета (/api/owner/*)

### 4. Frontend
Новые компоненты:
- `OwnerAuth.tsx` - Формы регистрации и логина
- `OwnerDashboard.tsx` - Личный кабинет владельца
- `OwnerApp.tsx` - Главный компонент личного кабинета
- `owner.html` - Отдельная точка входа для ЛК
- `owner-entry.tsx` - Entry point

## 🚀 Инструкции по развертыванию

### Шаг 1: Установка зависимостей

\`\`\`bash
npm install
\`\`\`

Уже установлено:
- bcryptjs (хэширование паролей)
- jsonwebtoken (JWT)
- Все остальные зависимости из package.json

### Шаг 2: Настройка PostgreSQL

1. **Запустить PostgreSQL** (если не запущен):
\`\`\`bash
sudo service postgresql start
# Или
pg_ctl start -D /path/to/data
\`\`\`

2. **Инициализировать БД**:
\`\`\`bash
node setup-db.js
\`\`\`

Это создаст:
- Пользователя `tabakapp`
- База данных `appdb`
- Все необходимые таблицы

### Шаг 3: Настройка переменных окружения

Создайте файл `.env.local`:

\`\`\`env
# Database
DATABASE_URL=postgresql://tabakapp:tabakpass123@localhost:5432/appdb

# JWT Secret (ОБЯЗАТЕЛЬНО измените в продакшене!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Gemini API (если используется)
GEMINI_API_KEY=your-gemini-api-key

# Telegram (для клиентов)
TELEGRAM_BOT_TOKEN=your-bot-token
VITE_TELEGRAM_BOT_USERNAME=your_bot_username
VITE_TELEGRAM_HTTPS_ONLY=false
VITE_TELEGRAM_AUTH_URL=http://localhost:3000/api/auth/telegram/callback
\`\`\`

### Шаг 4: Запуск в режиме разработки

Нужно запустить 2 процесса:

**Терминал 1 - Backend API**:
\`\`\`bash
npm run api
\`\`\`
Запустит Express сервер на порту 3000

**Терминал 2 - Frontend Dev Server**:
\`\`\`bash
npm run dev
\`\`\`
Запустит Vite dev server на порту 3000 (с проксированием на API)

### Шаг 5: Доступ к приложению

- **Клиентское приложение** (для пользователей): http://localhost:3000/
- **Личный кабинет** (для владельцев): http://localhost:3000/owner.html

## 📖 Использование API

### Регистрация владельца

\`\`\`bash
curl -X POST http://localhost:3000/api/auth/register \\
  -H "Content-Type: application/json" \\
  -d '{
    "email": "owner@example.com",
    "password": "password123",
    "fullName": "Иван Иванов",
    "phone": "+7 900 123 45 67"
  }'
\`\`\`

Ответ:
\`\`\`json
{
  "success": true,
  "owner": {
    "id": "uuid",
    "email": "owner@example.com",
    "fullName": "Иван Иванов",
    "phone": "+7 900 123 45 67",
    "emailVerified": false
  },
  "accessToken": "jwt-token",
  "refreshToken": "refresh-token"
}
\`\`\`

### Логин

\`\`\`bash
curl -X POST http://localhost:3000/api/auth/login \\
  -H "Content-Type: application/json" \\
  -d '{
    "email": "owner@example.com",
    "password": "password123"
  }'
\`\`\`

### Создание заявки на подключение

\`\`\`bash
curl -X POST http://localhost:3000/api/owner/applications \\
  -H "Content-Type: application/json" \\
  -H "Authorization: Bearer <your-access-token>" \\
  -d '{
    "venueName": "Кальянная Дым",
    "city": "Москва",
    "address": "ул. Примерная, д. 1",
    "phone": "+7 900 000 00 00",
    "description": "Уютная кальянная в центре города"
  }'
\`\`\`

### Получение списка заведений владельца

\`\`\`bash
curl http://localhost:3000/api/owner/venues \\
  -H "Authorization: Bearer <your-access-token>"
\`\`\`

## 🔒 Безопасность

### Что реализовано:
- ✅ Хэширование паролей (bcrypt, 10 rounds)
- ✅ JWT токены (access: 15 мин, refresh: 7 дней)
- ✅ Refresh token rotation
- ✅ Защита всех owner endpoints (requireAuth middleware)
- ✅ Валидация email и паролей
- ✅ SQL injection protection (параметризованные запросы)

### Что нужно добавить для продакшена:
- [ ] HTTPS (обязательно!)
- [ ] Rate limiting (например, express-rate-limit)
- [ ] Email подтверждение
- [ ] 2FA (опционально)
- [ ] CORS настройки (ограничить домены)
- [ ] Helmet.js для безопасности заголовков
- [ ] Логирование (Winston/Morgan)
- [ ] Мониторинг сессий

## 🗂️ Структура проекта

\`\`\`
/workspaces/Tabak_Multiuser/
├── server/
│   ├── api.js               # Главный API файл (обновлен)
│   ├── authService.js       # NEW: JWT, bcrypt
│   ├── authMiddleware.js    # NEW: Защита роутов
│   ├── authRoutes.js        # NEW: /api/auth/*
│   ├── ownerRoutes.js       # NEW: /api/owner/*
│   └── telegramAuth.js      # Существующая Telegram авторизация
├── components/
│   ├── OwnerAuth.tsx        # NEW: Формы входа/регистрации
│   ├── OwnerDashboard.tsx   # NEW: Личный кабинет
│   └── ...                  # Существующие компоненты для клиентов
├── OwnerApp.tsx             # NEW: Главный компонент ЛК
├── owner.html               # NEW: HTML для ЛК
├── owner-entry.tsx          # NEW: Entry point ЛК
├── setup-db.js              # Обновлен с новыми таблицами
├── vite.config.ts           # Обновлен (multi-page + proxy)
├── ARCHITECTURE.md          # NEW: Документация архитектуры
└── DEPLOYMENT.md            # Этот файл
\`\`\`

## 🧪 Тестирование

### 1. Проверка БД
\`\`\`bash
psql -U tabakapp -d appdb -c "\\dt"
\`\`\`

Должны быть видны таблицы:
- venues
- venue_owners
- venue_applications
- owner_sessions
- flavors
- brands
- clients
- mixes

### 2. Тестирование API

Используйте Postman, curl или Thunder Client для VS Code.

Коллекция endpoints:
- POST /api/auth/register
- POST /api/auth/login
- POST /api/auth/refresh
- POST /api/auth/logout
- GET /api/auth/me
- GET /api/owner/profile
- PATCH /api/owner/profile
- GET /api/owner/venues
- POST /api/owner/applications
- GET /api/owner/applications
- PATCH /api/owner/venues/:id
- GET /api/owner/venues/:id/stats

### 3. Тестирование UI

1. Откройте http://localhost:3000/owner.html
2. Зарегистрируйте нового владельца
3. Создайте заявку на подключение заведения
4. Проверьте, что заявка появилась в списке

## 🚧 Следующие шаги

### Обязательно для продакшена:
1. Админ-панель для одобрения заявок
2. Email уведомления (nodemailer)
3. Загрузка лого заведения (multer + S3/local storage)
4. Управление вкусами в ЛК
5. Статистика и аналитика

### Опционально:
- Система подписок и оплаты
- Push-уведомления
- Экспорт данных (Excel/CSV)
- Интеграция с CRM

## 🐛 Troubleshooting

### PostgreSQL не подключается
\`\`\`bash
# Проверка статуса
pg_isready

# Запуск
sudo service postgresql start

# Проверка порта
sudo lsof -i :5432
\`\`\`

### JWT ошибки
- Убедитесь, что JWT_SECRET установлен в .env.local
- Проверьте, что токен не истек
- Используйте /api/auth/refresh для обновления

### CORS ошибки
- Проверьте, что сервер API запущен
- Убедитесь, что proxy настроен в vite.config.ts

## 📞 Поддержка

При возникновении проблем проверьте:
1. Логи сервера (в терминале где запущен `npm run api`)
2. Browser console (F12)
3. Network tab в DevTools

---

**Готово к использованию!** 🎉

Система полностью функциональна и готова к локальному тестированию.
